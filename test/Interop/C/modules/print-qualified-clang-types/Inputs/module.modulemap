module ForeignA {
  // Nest the header in a sub-module to make sure these are handled correctly.
  module Sub {
    header "foreign-a.h"
  }
}

module ForeignB {
  // Nest the header in a sub-module to make sure these are handled correctly.
  module Sub {
    header "foreign-b.h"
  }
}
