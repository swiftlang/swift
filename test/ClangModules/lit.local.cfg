# Make a local copy of the substitutions.
config.substitutions = list(config.substitutions)

# Enable source imports for all tests in this suite.
config.substitutions.insert(0, ('%swift ', '%swift -enable-source-import ') )
