// Make sure that we can deserialize the stdlib.
// RUN: %sil-opt -verify %libdir/swift/macosx/Swift.swiftmodule -module-name=Swift -target=%target-triple > /dev/null
// RUN: llvm-bcanalyzer %libdir/swift/macosx/Swift.swiftmodule | FileCheck %s

// CHECK-NOT: Unknown
