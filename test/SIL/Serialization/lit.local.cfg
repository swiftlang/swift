# Make a local copy of the substitutions.
config.substitutions = list(config.substitutions)

module_dir = '%libdir/swift/%target-sdk-name'
if 'PTRSIZE=32' in config.available_features:
    module_dir += '/32'
config.substitutions.insert(0, ('%platform-module-dir', module_dir) )
