// RUN: %swift %s -emit-sil

// CHECK: sil clang_thunk @prototype :
sil clang_thunk @prototype : $() -> ()


// CHECK: sil @normal :
sil @normal : $() -> () {
}

// CHECK: sil internal @internal_fn :
sil internal @internal_fn : $() -> () {
}
