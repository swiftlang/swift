// RUN: %target-sil-opt %s -verify
// REQUIRES: asserts

sil_stage canonical

protocol Proto {
}

sil  @test : $@thin (@out protocol<>, @in Proto) -> () {
bb0(%0 : $*protocol<>, %1 : $*Proto):
  // init_existential should be able to put an existential container inside an existential container
  %2 = init_existential %0 : $*protocol<>, $Proto
  copy_addr [take] %1 to [initialization] %2 : $*Proto
  %4 = tuple ()
  return %4 : $()
}

