# Skipped serialization remarks (ModuleSerialization)

Emits remarks when the serializer skips invalid constructs.

## Overview

These remarks can be enabled with -Rmodule-serialization. Each construct highlighted because of these remarks should also have an associated error emitted (regardless of whether the remark is enabled).
