# Macro expansion remarks (MacroExpansions)

Emits a remark for each line in a macro expansion.

## Overview

Enable using `-Rmacro-expansion`. This option is intended for testing purposes and is not exposed in the compiler driver. See also `-dump-macro-expansion`.

