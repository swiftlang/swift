# Macro loading remarks (MacroLoading)

Emits a remark for each loaded macro implementation.

## Overview

Enable using `-Rmacro-loading`. This can be used to debug macro issues by verifying that the compiler found and loaded the macro.

