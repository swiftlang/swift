# Module loading remarks (ModuleLoading)

Emits a remark about a loaded module.

## Overview

Enable using `-Rmodule-loading`. Used for debugging which module an import is resolved to, and where it's located.
