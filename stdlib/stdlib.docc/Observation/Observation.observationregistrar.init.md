# ``Observation/ObservationRegistrar/init()``

@Metadata {
    @DocumentationExtension(mergeBehavior: override)
}

Creates an instance of the observation registrar.

You don't need to create an instance of ``Observation/ObservationRegistrar`` when
using the ``Observation/Observable()`` macro to indicate observably
of a type.
