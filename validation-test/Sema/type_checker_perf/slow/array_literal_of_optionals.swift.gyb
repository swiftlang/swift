// RUN: %scale-test --invert --begin 1 --end 5 --step 1 --select NumConstraintScopes %s
// REQUIRES: asserts,no_asan

func test() {
  let _ = [
    Optional(0),
  %for i in range(1, N):
    Optional(0),
  %end
  ]
}
