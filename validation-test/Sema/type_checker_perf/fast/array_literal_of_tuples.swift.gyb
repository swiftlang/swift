// RUN: %scale-test --begin 1 --end 10 --step 1 --select NumConstraintScopes %s
// REQUIRES: asserts,no_asan

func test() {
  let _ = [
    (0, "", false, 0.0),
  %for i in range(1, N):
    (0, "", false, 0.0),
  %end
  ]
}
