// RUN: %scale-test --begin 2 --end 6 --step 1 --select NumLeafScopes %s
// REQUIRES: asserts

let tuple: (UInt8
%for i in range(0, N):
  , UInt8
%end
) = (UInt8(0)
%for i in range(0, N):
  , UInt8(0)
%end
)
