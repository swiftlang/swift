// RUN: %scale-test --begin 1 --end 20 --step 1 --select NumLeafScopes %s
// REQUIRES: OS=macosx
// REQUIRES: asserts

% array_elements = 20

let _ = [
%for i in range(0, N):
  ${i} : [
    %for j in range(array_elements):
      "e${j}",
    %end
  ],
%end
]
