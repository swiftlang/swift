// RUN: %scale-test --begin 1 --end 8 --step 1 --select NumLeafScopes %s -Xfrontend=-typecheck
// REQUIRES: asserts, no_asan

// https://github.com/swiftlang/swift/issues/49635

func slow() {
  (Optional(1) ?? 1) * 1
%for i in range(0, N):
  * 1
%end
}
