// RUN: for x in %platform-sdk-overlay-dir/*.swiftmodule; do [[ $(basename "$x") = Swift.swiftmodule ]] && continue; llvm-bcanalyzer $x | FileCheck %s; %target-sil-opt -verify $x -sdk $(xcrun -show-sdk-path -sdk macosx) > /dev/null; done

// CHECK-NOT: Unknown
